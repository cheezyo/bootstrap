<p id="notice"><%= notice %></p>

<h1>Type Of Tranings</h1>

<table>
  <thead>
    <tr>
      <th>Type t</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @type_of_tranings.each do |type_of_traning| %>
      <tr>
        <td><%= type_of_traning.type_t %></td>
        <td><%= link_to 'Show', type_of_traning %></td>
        <td><%= link_to 'Edit', edit_type_of_traning_path(type_of_traning) %></td>
        <td><%= link_to 'Destroy', type_of_traning, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Type Of Traning', new_type_of_traning_path %>
