<%= form_with(model: utr_report, local: true) do |form| %>
  <% if utr_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(utr_report.errors.count, "error") %> prohibited this utr_report from being saved:</h2>

      <ul>
        <% utr_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :utr_date %>
    <%= form.date_select :utr_date %>
  </div>

  <div class="field">
    <%= form.label :boys %>
    <%= form.text_area :boys %>
  </div>

  <div class="field">
    <%= form.label :girls %>
    <%= form.text_area :girls %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
